# Deps for resident geo-web-crawl service + Doubao handler import chain.
# playwright is installed separately in the Dockerfile (official PyPI; mirrors often 403).
# Image is Python 3.12 to match backend requires-python; Browserless provides Chromium.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
sqlalchemy>=2.0.36
pgvector>=0.3.6
beautifulsoup4>=4.12.0
httpx>=0.28.0
publicsuffix2>=1.0.0
validators>=0.34.0
