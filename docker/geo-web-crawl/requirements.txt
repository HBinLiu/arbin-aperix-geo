# Lean deps for geo-web-crawl only (Doubao Playwright jobs + FastAPI).
# Do NOT mirror full backend/pyproject.toml — keep crawl import graph free of
# redis/celery/openai (see crawl_accounts/__init__ and utils.cache lazy loads).
# playwright is installed separately (China mirrors often 403 the wheel).
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
httpx>=0.28.0
beautifulsoup4>=4.12.0
publicsuffix2>=1.0.0
validators>=0.34.0
