# Deps for resident geo-web-crawl service + Doubao handler import chain.
# Pin playwright to the base image tag (mcr.microsoft.com/playwright/python:v1.49.1-jammy).
# Browserless mode only needs the Python package; local Chromium is already in the base image.
playwright==1.49.1
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
sqlalchemy>=2.0.36
pgvector>=0.3.6
beautifulsoup4>=4.12.0
httpx>=0.28.0
publicsuffix2>=1.0.0
validators>=0.34.0
