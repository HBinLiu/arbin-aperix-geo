export const queryKeys = {
  subjects: ["subjects"] as const,
  me: ["auth", "me"] as const,
  overview: (subjectId: string, from: string, to: string) =>
    ["overview", subjectId, from, to] as const,
  rank: (subjectId: string, from: string, to: string) =>
    ["rank", subjectId, from, to] as const,
  analysisEntities: (subjectId: string) => ["analysis-entities", subjectId] as const,
  analysisOverview: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["analysis-overview", subjectId, entityId, platformId, topicId, from, to] as const,
  analysisRank: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["analysis-rank", subjectId, entityId, platformId, topicId, from, to] as const,
  visibilityAnalysis: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["visibility-analysis", subjectId, entityId, platformId, topicId, from, to] as const,
  analysisPrompts: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["analysis-prompts", subjectId, entityId, platformId, topicId, from, to] as const,
  analysisTopics: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["analysis-topics", subjectId, entityId, platformId, topicId, from, to] as const,
  analysisPlatforms: (subjectId: string, from: string, to: string) =>
    ["analysis-platforms", subjectId, from, to] as const,
  platformMatrix: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["platform-matrix", subjectId, entityId, platformId, topicId, from, to] as const,
  analysisCitationRank: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["analysis-citation-rank", subjectId, entityId, platformId, topicId, from, to] as const,
  citationAnalysis: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["citation-analysis", subjectId, entityId, platformId, topicId, from, to] as const,
  citationDomainAnalysis: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    host: string,
    from: string,
    to: string,
  ) =>
    ["citation-domain-analysis", subjectId, entityId, platformId, topicId, host, from, to] as const,
  analysisDailySentiment: (subjectId: string, from: string, to: string) =>
    ["analysis-daily-sentiment", subjectId, from, to] as const,
  sentimentAnalysis: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["sentiment-analysis", subjectId, entityId, platformId, topicId, from, to] as const,
  contentOpportunities: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["content-opportunities", subjectId, entityId, platformId, topicId, from, to] as const,
  backlinkOpportunities: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["backlink-opportunities", subjectId, entityId, platformId, topicId, from, to] as const,
  diagnosis: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    from: string,
    to: string,
  ) => ["diagnosis", subjectId, entityId, platformId, topicId, from, to] as const,
  pipelineStatus: (subjectId: string) => ["pipeline-status", subjectId] as const,
  samplingJob: (jobId: string) => ["sampling-job", jobId] as const,
  samplingPlatforms: ["sampling-platforms"] as const,
  brandCompetitors: (subjectId: string) => ["brand-competitors", subjectId] as const,
  brandPrompts: (subjectId: string) => ["brand-prompts", subjectId] as const,
  subjectTopics: (subjectId: string) => ["subject-topics", subjectId] as const,
  promptDetailVisibility: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    ["prompt-detail-visibility", subjectId, entityId, platformId, topicId, promptId, from, to] as const,
  promptDetailCitation: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    ["prompt-detail-citation", subjectId, entityId, platformId, topicId, promptId, from, to] as const,
  promptDetailPlatforms: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    ["prompt-detail-platforms", subjectId, entityId, platformId, topicId, promptId, from, to] as const,
  promptDetailPrompts: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    ["prompt-detail-prompts", subjectId, entityId, platformId, topicId, promptId, from, to] as const,
  promptDetailOpportunities: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    [
      "prompt-detail-opportunities",
      subjectId,
      entityId,
      platformId,
      topicId,
      promptId,
      from,
      to,
    ] as const,
  promptDetailResponses: (
    subjectId: string,
    entityId: string,
    platformId: string,
    topicId: string,
    promptId: string,
    from: string,
    to: string,
  ) =>
    ["prompt-detail-responses", subjectId, entityId, platformId, topicId, promptId, from, to] as const,
  llmResponse: (responseId: string) => ["llm-response", responseId] as const,
};
